declare module 'midtrans-client' {
  // Basic declaration to satisfy TypeScript import resolution.
  // This tells TypeScript the module exists and treats it as 'any'.
  // You can expand this with more specific types if needed later.
  const midtransClient: any;
  export default midtransClient;
}
